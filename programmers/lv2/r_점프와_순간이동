#goal: 건전지 사용을 최소화(k칸점프=k만큼 건전지 사용, 순간이동=건전지 사용 x)
# 점프 최소화=> 건전지 사용줄임, ㄴ순간이동=현재까지 온 거리*2
# 다시 풀기!(사고하는 방법이 부족했음)

def solution(n):
    ans = 0
    
    while n != 0 :
        if n%2 == 0: #짝수==점프구간 x
            n /= 2 #n을 2로 나눠줘야 함(점프구간 ㅇ)
        else:
            #홀 수(건전지 사용구간)
            ans+=1
            n -= 1 #한 칸 옮긴거임.(==그만큼 빼줘야 함)
            
    return ans